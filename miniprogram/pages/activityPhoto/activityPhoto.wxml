<view wx:for='{{activityPhoto}}' wx:key>
  <view class='imageView' bindtap='toActivityPhoto' data-id='{{index}}'>
  <image src='{{item.cover}}' class='image'></image>
  </view>
  <text class='text'>{{item.introduce}}</text>
  <view class='aline' wx:if="{{index<items.length-1}}"></view>
</view>